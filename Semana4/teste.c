#include <stdio.h>
#include <stdlib.h>

struct estrutura{
    int valor;
    int *prox;
};

void criaLista(struct estrutura *Estrutura){
    alocador(Estrutura

int main() {
    struct estrutura *Estrutura;
    criaLista(Estrutura);
    return 0;
}
